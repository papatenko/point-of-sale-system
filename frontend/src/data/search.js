export const SEARCH_TABLES = [
  {
    endpoint: "/api/menu-items",
    type: "Menu Item",
    idField: "menu_item_id",
    titleField: "item_name",
    searchFields: ["item_name", "description", "category_name", "price"],
    getDetails: (item) => [item.category_name, item.price != null ? `$${item.price}` : null].filter(Boolean).join(" - "),
  },
  {
    endpoint: "/api/ingredients",
    type: "Ingredient",
    idField: "ingredient_id",
    titleField: "ingredient_name",
    searchFields: ["ingredient_name", "category", "unit_of_measure", "preferred_supplier_name"],
    getDetails: (item) => [item.category, item.unit_of_measure, item.preferred_supplier_name].filter(Boolean).join(" - "),
  },
  {
    endpoint: "/api/employees",
    type: "Employee",
    idField: "email",
    titleField: "first_name",
    searchFields: ["first_name", "last_name", "email", "role", "license_plate"],
    getDetails: (item) => [item.role, item.email, item.license_plate].filter(Boolean).join(" - "),
    getTitle: (item) => `${item.first_name || ""} ${item.last_name || ""}`.trim(),
  },
  {
    endpoint: "/api/suppliers",
    type: "Supplier",
    idField: "supplier_id",
    titleField: "supplier_name",
    searchFields: ["supplier_name", "contact_person", "email", "phone_number", "address"],
    getDetails: (item) => [item.contact_person, item.email, item.phone_number].filter(Boolean).join(" - "),
  },
  {
    endpoint: "/api/users",
    type: "User",
    idField: "email",
    titleField: "first_name",
    searchFields: ["first_name", "last_name", "email", "phone_number"],
    getDetails: (item) => [item.email, item.phone_number].filter(Boolean).join(" - "),
    getTitle: (item) => `${item.first_name || ""} ${item.last_name || ""}`.trim() || item.email,
  },
  {
    endpoint: "/api/trucks",
    type: "Food Truck",
    idField: "license_plate",
    titleField: "truck_name",
    searchFields: ["truck_name", "license_plate", "location", "phone_number"],
    getDetails: (item) => [item.license_plate, item.location, item.phone_number].filter(Boolean).join(" - "),
  },
  {
    endpoint: "/api/recipes",
    type: "Recipe",
    idField: "recipe_id",
    titleField: "recipe_name",
    searchFields: ["recipe_name", "ingredient_name", "menu_item_name"],
    getDetails: (item) => [item.menu_item_name, item.ingredient_name].filter(Boolean).join(" - "),
  },
  {
    endpoint: "/api/customers",
    type: "Customer",
    idField: "email",
    titleField: "first_name",
    searchFields: ["first_name", "last_name", "email", "phone_number"],
    getDetails: (item) => [item.email, item.phone_number].filter(Boolean).join(" - "),
    getTitle: (item) => `${item.first_name || ""} ${item.last_name || ""}`.trim() || item.email,
  },
];
